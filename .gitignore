# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Secrets / env
.env
.env.local

# OS / IDE
.DS_Store
.idea/
.vscode/

# Frontend (React/Vite). node_modules is huge + regenerable; dist is the build
# (we build into static/, which IS committed so the server can serve it).
frontend/node_modules/
frontend/dist/

# Claude Code internals
.claude/
.conductor/
memory/

# Audio cue clips (pre-generated by Agent C — large, regeneratable)
static/cues/*.mp3
